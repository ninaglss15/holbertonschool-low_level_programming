#include <stdio.h>
#include <ctype.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	int num;

	for (num = 'a'; num <= 'z'; num++)
	putchar('\n');
	putchar(num);
	return (0);
}
