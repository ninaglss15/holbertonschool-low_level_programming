#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - check the code
 *
 *@s: dfdxcesfsqfqre
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{

	}
	return (length);
}
