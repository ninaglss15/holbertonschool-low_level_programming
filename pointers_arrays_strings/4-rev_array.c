#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * reverse_array - check the code
 *
 *@a: dfdxcesfsqfqre
 *
 *@n: rjrbcj
 *
 * Return: Always 0.
 */
void reverse_array(int *a, int n)
{
	int length = 0;


	for (; s[length] != '\0'; length++)
	{
	}
	for (; length > 0; length--)
	{
	_putchar(s[length - 1]);
	}

	_putchar('\n');
}

