#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase and then in uppercase.
 *
 * Return: Always 0.
 */
int main(void)
{
int numbers;
for (numbers = '0'; numbers <= '10'; numbers++)
putchar(letter);
putchar('\n');
return (0);
}
